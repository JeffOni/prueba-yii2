# Database Configuration
# -----------------------------------------
# Host de la base de datos (usar 'mysql' para Docker, 'localhost' para Laragon local)
DB_HOST=mysql
# Nombre de la base de datos
DB_NAME=inventory_system
# Usuario de la base de datos
DB_USER=yii2user
# Contraseña de la base de datos
DB_PASSWORD=yii2pass

# MySQL Root Password
# -----------------------------------------
# Contraseña del usuario root de MySQL (solo para Docker)
MYSQL_ROOT_PASSWORD=verysecret

# Application Configuration
# -----------------------------------------
# Llave de encriptación para cookies (cambiar en producción)
COOKIE_VALIDATION_KEY=your-secret-cookie-validation-key-here

# Admin User Credentials (Default)
# -----------------------------------------
# Usuario administrador por defecto
ADMIN_EMAIL=admin@example.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin123!

# JWT Configuration (para API RESTful)
# -----------------------------------------
JWT_SECRET_KEY=your-jwt-secret-key-here
# Tiempo de expiración del JWT en segundos (default: 3600 = 1 hora)
JWT_EXPIRE_TIME=3600

# Application Environment
# -----------------------------------------
# Ambiente de la aplicación (dev, prod, test)
YII_ENV=dev
# Modo debug (1 = activado, 0 = desactivado)
YII_DEBUG=1

# Frontend URL
# -----------------------------------------
FRONTEND_URL=http://localhost:20080

# Backend URL
# -----------------------------------------
BACKEND_URL=http://localhost:21080
